<configuration>
    <property name="LOG_FILE_PATH" value="logs/"/>
    <property name="LOG_FILE_NAME" value="application.log"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>%-5level %d{yyyy-MM-dd HH:mm:ss} %logger{36} - [%X{logId}] - %msg%n</Pattern>
        </layout>
    </appender>

    <appender name="APPLICATION_LOGS" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE_PATH}${LOG_FILE_NAME}</file>
        <encoder>
            <pattern>%-5level %d{yyyy-MM-dd HH:mm:ss} %logger{36} - [%X{logId}] - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE_PATH}/${LOG_FILE_NAME}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <root level="INFO">
        <appender-ref ref="APPLICATION_LOGS" />
    </root>
    <root level="ERROR">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
